# 04. Стратегии

## Основной принцип

**Нет универсальной стратегии.**

Каждый инструмент имеет свою уникальную стратегию, адаптированную под его характеристики.

---

## Почему нет универсальной стратегии?

### Пример: XAUUSD vs US30

**Если использовать одну стратегию для обоих**:

```
Стратегия: BOS на H1 → OB на M15 → Entry

XAUUSD:
✅ H1 BOS четкие
✅ M15 OB отрабатывают
✅ Win Rate: 64%

US30:
❌ H1 BOS слабые вне NY session
❌ M15 OB часто ложные
❌ Win Rate: 25%
```

**Вывод**: То, что работает для золота, НЕ работает для индекса.

---

## Smart Money Concepts (SMC)

Все стратегии основаны на SMC принципах, но **применяют их по-разному**.

### Основные концепции SMC:

#### 1. BOS (Break of Structure)
Пробой структуры — сигнал о направлении тренда.

**Как это выглядит**:
```
Bullish BOS:
High1 ---> High2 (выше High1) = BOS UP

Bearish BOS:
Low1 ---> Low2 (ниже Low1) = BOS DOWN
```

**Применение**:
- XAUUSD: BOS на H1 → сильный сигнал
- US30: BOS надежен только на NY session

---

#### 2. Order Block (OB)
Зона, где институциональные игроки разместили ордера.

**Определение**:
- Последняя свеча перед импульсным движением
- "Откуда начался сильный move"

**Типы**:
- **Bullish OB**: Зона для BUY (после падения)
- **Bearish OB**: Зона для SELL (после роста)

**Применение**:
- XAUUSD: OB на M15 часто отрабатывают
- US30: Нужна дополнительная фильтрация (сессия, объем)

---

#### 3. Fair Value Gap (FVG)
Дисбаланс цены — "пропущенная зона".

**Определение**:
Три свечи, где средняя создала gap (не пересекается с соседними).

**Применение**:
- Цена часто возвращается в FVG перед продолжением движения
- XAUUSD: FVG надежны
- US30: Менее надежны (часто игнорируются)

---

#### 4. Liquidity Grab
Захват ликвидности перед истинным движением.

**Как это выглядит**:
```
1. Цена пробивает High/Low (захватывает стопы)
2. Быстро разворачивается
3. Идет в противоположном направлении (истинное движение)
```

**Применение**:
- Используется как подтверждение направления
- Особенно важно на инструментах с высокой ликвидностью (XAUUSD, EURUSD)

---

## Подход Multi-Timeframe (MTF)

Все стратегии используют анализ нескольких таймфреймов:

### Структура MTF:

```
HTF (Higher Time Frame)
   ↓
Setup Time Frame
   ↓
Trigger Time Frame (опционально)
```

### Пример для XAUUSD:

**HTF (H1)**: Определение направления
- Ищем BOS на H1
- Если BOS UP → торгуем только LONG
- Если BOS DOWN → торгуем только SHORT

**Setup (M15)**: Поиск зоны входа
- Ищем OB или FVG на M15
- Зона должна быть в направлении HTF

**Trigger (M5)**: Подтверждение
- Mini-BOS на M5 в зоне OB
- Engulfing pattern
- Rejection wick

---

## Baseline стратегия для XAUUSD

### Описание
Baseline — проверенная стратегия для золота, будет перенесена из предыдущего проекта.

### Логика:

#### Шаг 1: HTF анализ (H1)
```python
if detect_bos_up(h1_data):
    direction = "LONG"
elif detect_bos_down(h1_data):
    direction = "SHORT"
else:
    direction = "NONE"  # Нет четкого направления
```

#### Шаг 2: Setup зоны (M15)
```python
if direction == "LONG":
    zones = find_bullish_ob(m15_data) + find_bullish_fvg(m15_data)
elif direction == "SHORT":
    zones = find_bearish_ob(m15_data) + find_bearish_fvg(m15_data)

# Фильтрация зон по качеству
valid_zones = filter_by_quality(zones)
```

#### Шаг 3: Trigger (M15)
```python
if price_in_zone(current_price, valid_zones):
    if detect_confirmation(m15_data):  # Mini-BOS, engulfing, rejection
        return SIGNAL(direction, entry, sl, tp)
```

### Параметры:
- **HTF**: H1 (минимум 50 баров для анализа)
- **Setup**: M15
- **Risk**: 1.0R per trade
- **RR**: 1:2 (SL vs TP)

### Результаты (исторические):
- **2023**: WR 64.7%, DD -6.44%
- **2024**: (будет протестировано)
- **2025**: (будет протестировано)

**Статус**: ⏳ В процессе переноса

---

## Стратегия для US30

### Отличия от XAUUSD:

#### 1. Фильтр по сессиям
```python
if not is_ny_session(current_time):
    return NO_SIGNAL  # Торгуем только NY open
```

#### 2. Более высокий HTF
```python
# Для индекса используем H4 вместо H1
direction = analyze_htf(h4_data)
```

#### 3. Дополнительная фильтрация OB
```python
# OB для индексов менее надежны
if ob.quality < 0.7:  # Более строгий порог
    skip_ob()
```

**Статус**: 🔮 Планируется разработка

---

## Как добавить новую стратегию

### Шаг 1: Создать файл
```
strategies/instrument/strategy.py
```

### Шаг 2: Реализовать класс
```python
class InstrumentStrategy:
    def __init__(self):
        # Параметры стратегии
        pass
    
    def analyze(self, htf_data, ltf_data):
        """
        Анализ рынка.
        
        Returns:
            dict: {
                'signal': 'BUY' | 'SELL' | 'NONE',
                'entry': float,
                'sl': float,
                'tp': float,
                'confidence': float
            }
        """
        # 1. HTF анализ → направление
        # 2. Setup зоны → где входить
        # 3. Trigger → когда входить
        pass
```

### Шаг 3: Описать в документации
- Добавить инструмент в `03_instruments.md`
- Описать логику в этом файле (04_strategies.md)

### Шаг 4: Тестировать
```python
# tests/backtests/run_instrument_2023.py
python tests/backtests/run_instrument_2023.py
```

### Шаг 5: Анализировать результаты
- Win Rate ≥ 50%?
- Max Drawdown ≤ 10%?
- Profit Factor ≥ 1.5?

Если нет → корректировать стратегию или отказаться от инструмента.

---

## Общие принципы всех стратегий

### 1. HTF определяет направление
Никогда не торгуем против HTF. Если HTF говорит DOWN, ищем только SHORT.

### 2. Setup определяет ГДЕ
Зоны входа (OB/FVG) должны быть в направлении HTF.

### 3. Trigger определяет КОГДА
Не входим "вслепую". Ждем подтверждения (mini-BOS, engulfing, rejection).

### 4. Risk Management
- 1.0R per trade (можно меньше, но не больше)
- SL всегда за зону (за OB или FVG)
- TP минимум 1.5R (лучше 2R)

### 5. No Optimization Hell
Если стратегия не работает с базовыми параметрами → не работает вообще.  
Не подгоняем параметры под исторические данные.

---

## Что НЕ делать

❌ **Не создавать "универсальную" стратегию**  
   → Каждый инструмент уникален

❌ **Не оптимизировать до бесконечности**  
   → Если базовая логика не работает, переделывать логику, а не параметры

❌ **Не игнорировать HTF**  
   → Торговля против HTF = верный путь к убыткам

❌ **Не торговать без подтверждения**  
   → "Цена в зоне" ≠ "вход". Нужен trigger.

---

## Эксперименты

Все эксперименты с модификациями стратегий документируются в `05_experiments.md`.

Пример:
```markdown
## Эксперимент #1: Фильтр по волатильности для XAUUSD
**Гипотеза**: Скип сигналов в периоды низкой волатильности
**Результат**: FAIL — WR снизился с 64% до 58%
**Вывод**: Не использовать
```

---

**Версия**: 1.0  
**Дата**: 17 декабря 2025
