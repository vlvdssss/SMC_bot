# Ручная Торговля BAZA

## Обзор

Система ручной торговли BAZA предоставляет полный набор инструментов для открытия сделок вручную с поддержкой AI-анализа и автоматического расчета рисков.

## Возможности

### 🎯 Основные Функции
- **Ручное открытие сделок** с полным контролем параметров
- **AI-анализ рынка** перед открытием позиции
- **Автоматический расчет** объема позиции и RR ratio
- **Валидация параметров** сделки перед исполнением
- **Интеграция с исполнителем** для реального открытия сделок

### 🔧 Компоненты Системы

#### Модели Данных (`src/models.py`)
- `TradeRequest` - запрос на открытие сделки
- `TradeResult` - результат выполнения сделки
- `AIPrediction` - прогноз AI для ручной торговли

#### Контроллер (`src/manual_trading/controller.py`)
Основной координатор всех компонентов ручной торговли.

#### Валидатор (`src/manual_trading/validator.py`)
Проверяет корректность параметров сделки:
- Базовые параметры (символ, направление, цены)
- Риск-менеджмент (максимальный риск, объем)
- Уровни цен (SL/TP валидность)

#### Калькулятор (`src/manual_trading/calculator.py`)
Автоматические расчеты:
- **Объем позиции** на основе риска и баланса
- **RR ratio** (risk-reward ratio)
- Поддержка различных валютных пар

#### AI Анализатор (`src/manual_trading/ai_analyzer.py`)
Формирует рыночные прогнозы на основе:
- Технического анализа
- Рыночных настроений
- Уровней поддержки/сопротивления

### 🎨 GUI Интерфейс

#### Элементы Управления
- **Селектор символа** (EURUSD, XAUUSD, etc.)
- **Таймфрейм** (M15, H1, etc.)
- **Направление** (BUY/SELL)
- **Ценовые уровни** (Entry, Stop Loss, Take Profit)
- **Риск** (в валюте счета)

#### Авторасчет
- **Объем позиции** рассчитывается автоматически
- **RR ratio** отображается в реальном времени
- Расчеты обновляются при изменении любого параметра

#### Кнопки Действий
- **🔮 Predict** - запуск AI-анализа
- **🚀 Открыть сделку** - исполнение сделки

### ⚙️ Конфигурация

Настройки в `config/portfolio.yaml`:

```yaml
manual_trading:
  enabled: true
  max_risk_percent: 2.0
  min_lot_size: 0.01
  max_lot_size: 10.0
```

### 🔒 Безопасность

- **Блокировка во время работы бота** - нельзя открывать ручные сделки пока бот активен
- **Валидация всех параметров** перед исполнением
- **Логирование всех операций** для аудита

### 📊 Логирование

Все операции ручной торговли логируются:
- Попытки открытия сделок
- Результаты AI-анализа
- Исполненные сделки
- Ошибки и предупреждения

## Использование

1. **Выберите символ и таймфрейм**
2. **Установите направление** (BUY/SELL)
3. **Введите ценовые уровни** (Entry, SL, TP)
4. **Укажите риск** в валюте счета
5. **Посмотрите авторасчет** объема и RR
6. **Нажмите Predict** для AI-анализа
7. **Нажмите "Открыть сделку"** для исполнения

## API

### ManualTradingController

```python
controller = ManualTradingController(config, executor, llm_client)

# Расчет объема
lot_size, message = controller.calculator.calculate_lot_size(...)

# Расчет RR
rr_ratio = controller.calculator.calculate_rr_ratio(...)

# AI прогноз
prediction = controller.get_ai_prediction(context)

# Открытие сделки
success, message = controller.execute_trade()
```

## Тестирование

```bash
# Запуск GUI
python -m src.gui.app

# Тест компонентов
python -c "from src.manual_trading.controller import ManualTradingController; ..."
```

## Архитектура

```
GUI Layer (tkinter)
    ↓
ManualTradingController
    ↓
├── TradeValidator
├── RiskCalculator
├── ManualAIAnalyzer
└── Executor (MT5)
```</content>
<parameter name="filePath">c:\Users\kamsa\Desktop\BAZA\MANUAL_TRADING_README.md